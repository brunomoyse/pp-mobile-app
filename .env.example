# ============================================
# PocketPair Mobile App (pp-mobile) Environment Configuration
# ============================================
# Copy this file to .env and configure for your environment

# ============================================
# GraphQL Configuration
# ============================================

# GraphQL HTTP endpoint
# Used for queries and mutations
# For local development: http://localhost:8080/graphql
# For production: https://api.your-domain.com/graphql
NUXT_GRAPHQL_ENDPOINT=http://localhost:8080/graphql

# GraphQL WebSocket endpoint
# Used for real-time subscriptions (tournament clocks, registrations, notifications)
# For local development: ws://localhost:8080/graphql
# For production with HTTPS: wss://api.your-domain.com/graphql
NUXT_GRAPHQL_WS_ENDPOINT=ws://localhost:8080/graphql

# ============================================
# Authentication Configuration
# ============================================

# Authentication base URL
# Points to the backend OAuth endpoints
NUXT_AUTH_BASE_URL=http://localhost:8080

# OAuth registration endpoint
# Used for new player account creation
NUXT_AUTH_REGISTER_URL=http://localhost:8080/oauth/register

# OAuth login endpoint
# Used for player authentication
NUXT_AUTH_LOGIN_URL=http://localhost:8080/oauth/login

# ============================================
# Optional Configuration
# ============================================

# API key (if implementing API key authentication)
# Not currently used, reserved for future use
# NUXT_API_KEY=your-api-key-here

# Enable GraphQL debugging in console (default: false)
# Set to true to see GraphQL query/mutation logs in browser DevTools
NUXT_DEBUG_GRAPHQL=false

# ============================================
# Mobile-Specific Notes
# ============================================
# - For iOS/Android builds with Capacitor, update these URLs to production endpoints
# - When testing on physical devices, replace 'localhost' with your computer's IP:
#   Example: http://192.168.1.100:8080/graphql
# - For production native apps, use HTTPS endpoints only
# - WebSocket subscriptions require ws:// for HTTP or wss:// for HTTPS
# - All NUXT_ prefixed variables are exposed to the client-side code

# ============================================
# Capacitor Development
# ============================================
# When running on a physical device or emulator:
# 1. Find your computer's local IP: ifconfig (macOS/Linux) or ipconfig (Windows)
# 2. Update all localhost references to your IP address
# 3. Ensure your device is on the same network as your development machine
# 4. Example for IP 192.168.1.100:
#    NUXT_GRAPHQL_ENDPOINT=http://192.168.1.100:8080/graphql
#    NUXT_GRAPHQL_WS_ENDPOINT=ws://192.168.1.100:8080/graphql